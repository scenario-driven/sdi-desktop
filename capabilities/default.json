{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — Tauri core only, no plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
